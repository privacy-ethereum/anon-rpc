{
  "name": "anon-rpc-impl",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "anon-rpc reference prototype: harness + passthrough worker (see ../SPEC.md).",
  "workspaces": [
    "browser-harness",
    "passthrough-worker",
    "test-worker",
    "specifier",
    "site"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:e2e": "node test/run-e2e.mjs"
  },
  "devDependencies": {
    "@kpstreams/server": "^0.1.0",
    "@noble/hashes": "^1.5.0",
    "esbuild": "^0.28.0",
    "playwright": "^1.48.0"
  }
}
