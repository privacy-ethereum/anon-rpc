{
  "name": "anon-rpc-test-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Test worker for the anon-rpc e2e: exercises fetch passthrough, KPS routing, and persistent storage. Not the template - copy ../passthrough-worker instead.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
