{
  "name": "anon-rpc-passthrough-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Template anon-rpc worker (SPEC.md §3.2): fetch passthrough + kps+echo routing. Copy this directory to start your own anon-client.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
